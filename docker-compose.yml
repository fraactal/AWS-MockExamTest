services:
  aws-prep-app:
    build: .
    container_name: aws-prep-app
    ports:
      - "3080:3080"
    environment:
      PORT: 3080
      DATABASE_PATH: /data/prep.db
    volumes:
      - ./runtime-data:/data
    restart: unless-stopped
